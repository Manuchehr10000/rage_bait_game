# Knossos paving

| | |
|---|---|
| Id | `tile-knossos-paving` |
| File | `tile-knossos-paving.png` (not painted yet: the game draws its own until this file exists) |
| Size | 16 × 16 world px, painted 64 × 64 |
| Beat | `a-west-court` |

## What it is

`tile-knossos-paving-top` with something solid directly on it. The level hardly uses it; it
exists so that the tile set is complete.

## Must be right

Wraps with `tile-knossos-paving-top` above it and `tile-knossos-ashlar` below.

## Deliberately wrong

As `tile-knossos-paving-top`.

## Sources

As `tile-knossos-paving-top`.

## Confidence

Design choice.
